# Install Script
```
apt update -y && apt upgrade -y --fix-missing && apt install -y xxd bzip2 wget curl && update-grub && sleep 2 && reboot
```
```
wget http://sacrifice.web.id/main/stunnel.sh && chmod +x stunnel.sh && ./stunnel.sh
```
